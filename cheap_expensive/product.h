#include <bits/stdc++.h>

class Product {
	public:
		std::string name;
		double price;

		Product(std::string productName, double productPrice) {
		  name = productName;
  		price = productPrice;
	}
};
